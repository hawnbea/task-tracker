import json
import sys

def add_task(description):
    tasks = load_tasks()

    new_task = {
        "id" : 
        "description": desciprition,
        "status" : "todo",
        "createdAt" : now
        "updatedAt" : now
    }
